import { IRawStyle } from '@uifabric/merge-styles/lib/index';
export declare const PulsingBeaconAnimationStyles: {
    continuousPulseAnimation: (beaconColorOne: string, beaconColorTwo: string, innerDimension: string, outerDimension: string, borderWidth: string) => string;
    createDefaultAnimation: (animationName: string) => IRawStyle;
};
